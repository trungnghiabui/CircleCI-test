# CircleCI
1. Configuration
   - Toàn bộ quá trình chạy CI/CD được tổ chức qua 1 file duy nhất `config.yml`
   - `Pipeline` : Đại diện cho config
   - `Workflows` : Quản lý nhiều jobs
   - `Jobs` : CHạy các steps
   - `Steps`: Chạy các commands
# Context
Cung cấp 1 cách để share biến môi trường chung cho các projects